# LangChain Calculator Agent Requirements

# Core LangChain - using specific version that works
langchain==0.3.7
langchain-community==0.3.5
langchain-core==0.3.15

# LangChain Hub for prompts
langchainhub>=0.1.15

# OpenAI package for OpenRouter API
openai>=1.10.0

# LangChain OpenAI integration
langchain-openai>=0.1.0

# HTTP client
requests>=2.31.0

# Required dependencies
pydantic>=2.0.0
pydantic-core>=2.0.0

# Optional but recommended for better output
colorama>=0.4.6
python-dotenv>=1.0.0

# Video processing
moviepy>=1.0.3

# Audio to text (Whisper)
openai-whisper>=20231117

# Ollama for translation
ollama>=0.1.0

# PyTorch for Whisper GPU acceleration
# IMPORTANT: The packages below will install CPU-only PyTorch by default.
# For GPU (CUDA) support, you MUST install PyTorch separately:
#
#   1. First uninstall CPU-only version:
#      pip uninstall torch torchvision torchaudio
#
#   2. Then install CUDA-enabled version (check your CUDA version with: nvidia-smi):
#      # For CUDA 12.1:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#      # For CUDA 11.8:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
#   3. Verify installation: python check_cuda.py
#
# For CPU-only systems, the packages below will work fine.
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0